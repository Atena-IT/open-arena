apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "open-arena.configMapName" . }}
  labels:
    {{- include "open-arena.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
