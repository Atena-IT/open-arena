# Open Arena Web

A small Vite + React + TypeScript dashboard skeleton for the Open Arena API.

## Start

```bash
cd demo/gui/frontend
npm install
npm run dev
```

By default the UI targets `http://127.0.0.1:8000` and uses the local development
API token (`open-arena-dev-token`).
